#ifndef OPTIONS_H
#define OPTIONS_H

#include <QWidget>

class Options : public QWidget {
    Q_OBJECT

public:
    Options(QWidget *options = nullptr);
    ~Options();

private:

};

#endif // OPTIONS_H
